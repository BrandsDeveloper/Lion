name: Acessos Workflow

on: [push]

jobs:
  verificar_acessos:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2.4.0

    - name: Configurar Variáveis de Ambiente
      run: |
        echo "DB_HOST_DEV=${{ secrets.DB_HOST_DEV }}"
        echo "DB_TABLE_DEV=${{ secrets.DB_TABLE_DEV }}"
        echo "DB_USER_DEV=${{ secrets.DB_USER_DEV }}"
        echo "DB_PASS_DEV=${{ secrets.DB_PASS_DEV }}"
